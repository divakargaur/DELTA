// trims whitespace from both ends of string and returns a new one

let str = " radhe radhe "
const s = str.trim()

console.log(str);           // methods do not change the original data rather it creates a new string
console.log(str.trim());
console.log(s);