// sorts an array

let colors = ['blue', 'red', 'yellow', 'green'];
console.log(colors.sort());

let num = [1, 34, 73, 154, 28466, 42684];
console.log(num.sort()); // coverts the numbers into strings and then sorts
