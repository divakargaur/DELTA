// copies a portion of an array
// .slice(START, END)
// END is not included

let colors = ['red', 'blue', 'yellow']

console.log(colors);
console.log(colors.slice());
console.log(colors.slice(1, 2));
console.log(colors.slice(-2)); // last two elements
